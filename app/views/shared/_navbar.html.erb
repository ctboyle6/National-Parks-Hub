<div class="navbar navbar-expand-sm navbar-light navbar-lewagon justify-content-end">
  <%= link_to "Home", "/", class: "nav-link" %>

  <nav>
    <% if user_signed_in? %>
        <%= link_to('Logout', destroy_user_session_path, method: :delete) %>          
      <% else %>
        <%= link_to('Log In', new_user_session_path)  %> |
        <%= link_to('Sign Up', new_user_registration_path)  %>  
      <% end %>
  </nav>

  <span style="margin-left: 12px">Welcome, <%= user_signed_in? ? current_user.email : 'Guest' %></span>
</div>
